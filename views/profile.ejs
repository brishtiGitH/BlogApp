<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white p-10">
    <div class="flex justify-between">
        <h1 class="text-xl">Profile</h1>
        <div class="links">
            <a href="/user/feed"
                class="logout bg-sky-500 text-black font-semibold py-2 text-sm px-5 mr-2 rounded-md">Feed</a>
            <a href="/logout"
                class="logout bg-yellow-500 text-black font-semibold text-sm py-2 px-5 rounded-md">Logout</a>
        </div>

    </div>
    <div class="form">
        <div class="pfp flex justify-start gap-2 my-3">
            <div class="profilePic w-10 h-10 rounded-md overflow-hidden">
                <img src="/images/profilePics/<%= user.profilePic %>" class="w-full h-full object-cover" alt="">
            </div>
            <p class="text-2xl">
                <%= user.name %>
            </p>
            <a href="/upload" class="bg-zinc-700 font-semibold text-zinc-300 text-sm py-1 px-3 rounded-md ">Change
                Profile Picture</a>
        </div>


        <form action="/post/create" method="post">
            <textarea class="border bg-transparent w-2/5 block mb-2 p-2 rounded-md resize-none text-sm" name="content"
                id="content" rows="3" cols="40" placeholder="What's on your mind?"></textarea>
            <button type="submit" class="bg-cyan-700 font-semibold text-black text-sm p-1 rounded-md ">Create
                Post</button>
        </form>
    </div>




    <p class="mt-5">Your posts.</p>

    <div id="posts" class="w-2/5 ">
        <% user.posts?.forEach(post=> { %>
            <div class="post border-[1px] border-slate-600 p-2 mb-2 rounded-md">
                <p>@<%= user.username %>
                </p>
                <p class=" text-gray-500">
                    <%= post.content %>
                </p>

                <a href="/edit/<%= post._id %>" class="text-blue">Edit</a>
                <a href="/post/delete/<%= post._id %>" class="text-red-500">Delete</a>
            </div>
            <% }) %>

    </div>
</body>

</html>